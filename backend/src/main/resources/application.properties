server.port=${PORT:8083}

# Configuración de la base de datos PostgreSQL con Neon Tech
# Usar el pooler de Neon con SSL requerido
spring.datasource.url=jdbc:postgresql://${DB_HOST:ep-delicate-sound-ailtkjb8-pooler.c-4.us-east-1.aws.neon.tech}:${DB_PORT:5432}/${DB_NAME:neondb}?sslmode=require&channel_binding=require
spring.datasource.username=${DB_USERNAME:neondb_owner}
spring.datasource.password=${DB_PASSWORD:npg_g9BpjO5woiJA}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.open-in-view=false

# Configuración de CORS
spring.servlet.cors.allowed-origins=${CORS_ORIGINS:http://localhost:3000}

# Configuración de JWT
app.jwt.secret=${JWT_SECRET:sidaf-puno-secret-key-2024-muy-largo-para-seguridad}
app.jwt.expiration=${JWT_EXPIRATION:86400000}
